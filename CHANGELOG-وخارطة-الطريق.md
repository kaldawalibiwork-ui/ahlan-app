# اهلا — سجل التطوير وخارطة الطريق التجارية
# Ahlan — Changelog & Commercial Roadmap

> نسخة التطوير: يوليو 2026 — فرع `claude/website-fixes-improvements-2xi3qy`

---

## ✅ ما تم إصلاحه وتطويره في هذه النسخة

### 1) البيانات اليدوية لم تكن تظهر في الملف النهائي
- **السبب:** الملاحظات والمهام المُدخلة يدوياً كانت تُحفظ لكن تبقى بلغة الإدخال، ولم تُترجم في PDF/الإنفوجرافيك؛ كما أن دالة حذف المهام المترجمة (`removeAICustomTask`) كانت **مفقودة** وتُسبب توقف الصفحة عند الحذف.
- **الحل:** أُضيفت الدالة المفقودة، وأصبح كل محتوى يدوي يُترجم تلقائياً ويُخزَّن بكل اللغات الست، ويُعرض بلغة القارئ في كل المخرجات.

### 2) الترجمة الداخلية للملاحظات اليدوية
- عند كتابة أي ملاحظة أو مهمة، تُترجم تلقائياً في الخلفية إلى (العربية، الإنجليزية، الهندية، الأمهرية، التجالوج، الإندونيسية) وتُخزَّن مع الملف.
- تُعرض بلغة القارئ **بدون إنترنت** (لأنها مخزّنة)، مع ترجمة فورية احتياطية عبر الشبكة إن لم تكن مخزّنة.
- لا تُخزَّن ترجمة فاشلة — يُعاد المحاولة تلقائياً لاحقاً.

### 3) اختيار اللغة في كل أدوات المشاركة
- منتقي لغة موحّد يظهر في: صفحة المشاركة، ونافذة الرابط/الكود، ونافذة QR — وأي اختيار يُطبَّق فوراً على **الرابط والكود وPDF والإنفوجرافيك** معاً.

### 4) الصور بجانب المهمة (لا في الأسفل)
- في PDF: كانت كل الوسائط تُكدّس في نهاية الملف. الآن تظهر صور كل غرفة/قسم **داخل قسمها مباشرة**، وصور المهام الفردية بجانب غرفتها. أُضيف أيضاً قسم «المنظفات» الذي كان غائباً عن PDF.

### 5) رابط المشاركة وكود المشاركة
- **السبب الجذري:** المكتبة المسؤولة عن ضغط الرابط/الكود (LZString) كانت تُحمَّل من CDN خارجي — وإن تأخّر أو حُجب، تتعطّل المشاركة كلياً.
- **الحل:** أُدمِجت المكتبة **داخل الملف** فأصبح الرابط والكود يعملان دائماً بلا اعتماد على أي خادم خارجي. (اختُبر تدوير الكود ذهاباً وإياباً بنجاح.)
- ملاحظة: الرابط لا يعمل للطرف الآخر إلا إذا كان الموقع منشوراً على **رابط عام** (راجع قسم النشر أدناه).

### 6) الإنفوجرافيك — إعادة تصميم كاملة
- تصميم جديد واضح من النظرة الأولى: ترويسة أنيقة + إحصاءات + «إيقاع اليوم» + مهام مجمّعة حسب الغرفة بأيقونات رسومية + الأجهزة + المناطق الممنوعة + القواعد (أخضر ✓ / أحمر ✕) + الطوارئ.
- **تصحيح خطأ:** كانت كل القواعد تظهر «ممنوع» بسبب قراءة حقل خاطئ (`rules.phone` بدل `rules.phoneAllowed`).
- دعم خطوط **Noto** لعرض الهندية والأمهرية وغيرها بشكل صحيح بدل المربعات الفارغة.

### 7) مقاطع الفيديو
- **السبب:** كانت تُخزَّن كرابط مؤقت (`blob URL`) يموت عند إعادة تحميل الصفحة.
- **الحل:** تُخزَّن الآن في **IndexedDB** فتبقى بعد إعادة التحميل وتُشغَّل مباشرة داخل الصفحة، مع تدرّج آمن عند تعذّر التخزين.

### 8) الأيقونات والرسومات
- رسومات SVG احترافية مدمجة (الصالة، المطبخ، الحمام، غرفة النوم، الفناء، الغسالة، المكنسة، الميكروويف، المكواة…) تظهر بوضوح في واجهة العامل والإنفوجرافيك.

### 9) حفظ بيانات المستخدم
- بيانات كل حساب تُحفظ محلياً باسم الحساب، مع شارة حساب واضحة وزر تسجيل خروج، وتحقّق أساسي من صيغة البريد/الجوال.

### تحسينات إضافية
- إزالة مكتبة `jsPDF` غير المستخدمة.
- تطبيق لغة الواجهة المحفوظة عند التشغيل.
- تأمين إضافي عند عرض بيانات مستوردة من كود مشاركة (منع تنفيذ سكربتات).

---

### 10) ⭐ الدليل التفاعلي الكامل (حل مشكلة عدم ظهور الصور/الفيديو عند المشاركة)
- **المشكلة:** الرابط والكود نصّيان فقط (لا يمكن حشو الصور/الفيديو فيهما بسبب الحجم)، فكان العامل لا يرى الصور ولا المقاطع.
- **الحل:** خيار جديد «**دليل تفاعلي كامل**» ينتج **ملف HTML واحد مستقل** يحوي كل شيء مضمّناً بداخله: كل الصور والفيديوهات + الأيقونات والرسومات + مبدّل اللغات الست + شرح صوتي — ويعمل **بلا إنترنت وبلا خادم**.
- تُرسله للعامل **كملف** عبر واتساب/إيميل، يفتحه فيرى دليل المنزل كاملاً بصرياً بلغته، ويشغّل الفيديوهات، ويستمع للشرح — دون أن يحتاج الرجوع إليك.
- **يعمل بصرياً بالكامل بلا إنترنت:** كل الأيقونات والرسومات والصور والفيديو مضمّنة داخل الملف (الأيقونات تتحوّل تلقائياً إلى SVG). (النصوص تستخدم خطوطاً من الإنترنت مع بديل نظامي عند عدم الاتصال.)
- يعرض حجم الملف عند الإنشاء (لأن الفيديو قد يكبّره)؛ ويستخدم نفس محرّك عرض واجهة العامل لضمان تطابق التصميم.
- (اختُبر: توليد الدليل، فتحه كملف مستقل، ظهور الصورة المضمّنة، تبديل اللغة، زر الاستماع — كلها تعمل.)

### 11) ⭐ إنفوجرافيك «مفهوم بالنظر» + أيقونات SVG موثوقة
- أُعيد تصميم الإنفوجرافيك ليُفهَم **بمجرد النظر لغير القارئ**:
  - المهام كـ**بطاقات بصرية كبيرة**: صورة المهمة الحقيقية (إن رفعتها) أو أيقونة كبيرة + **رقم تسلسلي** لكل مهمة.
  - **صورك تظهر داخل الإنفوجرافيك** (الصورة أوضح رسالة لغير القارئ).
  - القواعد كـ**إشارة مرور**: بطاقة خضراء ✓ (مسموح) / حمراء ✕ (ممنوع) مع مفتاح ألوان يشرح المعنى.
  - المنظفات: مسموح (أخضر) / **ممنوع بتحذير أحمر** ⚠️. مناطق ممنوعة 🚫 بوضوح.
- **إصلاح جوهري للأيقونات:** كانت تعتمد على «خط أيقونات» لا يظهر دائماً في الصورة/الطباعة. الآن كل الأيقونات **SVG مضمّنة** (١٣٢ أيقونة) تظهر **دائماً — بلا إنترنت وفي الصورة والطباعة**.
- ملف الـPDF أيضاً لم يكن يُحمّل خط الأيقونات إطلاقاً (كانت مخفية) — أصبحت كلها تظهر الآن.

---

## ⚠️ الحقيقة حول «الحماية من القرصنة» و«الجاهزية التجارية»

الموقع الحالي **ملف HTML واحد يعمل بالكامل داخل متصفح الزائر**. هذا يعني بصدق:

- **لا يمكن جعله «غير قابل للنسخ» تقنياً** — أي كود يصل للمتصفح يمكن رؤيته وحفظه. أي «تشفير» للواجهة الأمامية هو تمويه يُفكّ بسهولة، وليس حماية حقيقية.
- **البيانات محفوظة على جهاز المستخدم فقط** (localStorage/IndexedDB) — تُفقد عند مسح المتصفح، ولا تتزامن بين الأجهزة.
- **لا يوجد تسجيل دخول حقيقي** — الحالي مجرد معرّف محلي بلا كلمة مرور أو تحقّق.

### الطريق الواقعي للنسخة التجارية المحمية (يتطلب خادماً backend)
1. **مصادقة حقيقية:** تسجيل دخول عبر OTP للجوال أو Google، برموز JWT.
2. **قاعدة بيانات سحابية** (Supabase أو Firebase): حفظ الملفات في السحابة، تزامن بين الأجهزة، نسخ احتياطي تلقائي.
3. **وسيط ترجمة على الخادم:** بدل نقطة Google المجانية غير الرسمية (قد تُحجب)، يمنحك ثباتاً وسريّة.
4. **رفع الوسائط للسحابة** (صور/فيديو) عبر تخزين كائني، فتُشارك بين الأجهزة (حالياً لا تُشارك عبر الرابط بسبب الحجم).
5. **التحكم بالوصول والاشتراكات:** خطط مدفوعة، حدود استخدام، لوحة إدارة.
6. **الحماية الحقيقية للملكية:** المنطق الحسّاس والبيانات على الخادم، والواجهة الأمامية مجرد عارض — هذا ما يمنع «السرقة» فعلياً.

> باختصار: يمكن أن يكون المنتج تجارياً واحترافياً وآمناً — لكن ذلك يتحقق عبر **خادم**، لا عبر إخفاء كود الواجهة.

---

## 🚀 النشر (Deployment)
لكي يعمل رابط المشاركة و QR لدى الطرف الآخر، يجب نشر الموقع على رابط عام (مثل Netlify).
عند النشر يصبح `الرابط = https://اسم-موقعك.netlify.app/#w=...&l=xx` قابلاً للفتح على أي جهاز.

---

## ✔️ كيف تم التحقق
اختُبر آلياً عبر متصفح Chromium: تشغيل الموقع بلا أخطاء، إنشاء ملف، إضافة مهمة يدوية وملاحظة، تدوير كود المشاركة (حُفظت المهمة العربية بنجاح)، بناء PDF بالهندية، ورسم واجهة العامل والإنفوجرافيك (لقطات شاشة مرفقة في المحادثة).
